namespace MathGame.Enums;

internal enum MenuOptions
{
    Addition,
    Soustration,
    Multiplication,
    Division,
    Random
}

internal enum DifficultyOption
{
    Easy,
    Medium,
    Hard,
    Extreme
}